# Optional: override MCP server port (defaults to 8000)
MCP_PORT=8000

# Optional: configure sqlite tool backing store and metadata.
# MCP_SQLITE_DB=./samples/sqlite/titanic.db
# MCP_SQLITE_METADATA=./samples/sqlite/titanic.yml
